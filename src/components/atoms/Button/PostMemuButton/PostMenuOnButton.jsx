import styled from 'styled-components';

const PostMenuOnButton = styled.button`
  position: absolute;
  z-index: 90;
  transition: 0.5s;
  transform: rotate(0deg);
`;

export default PostMenuOnButton;
