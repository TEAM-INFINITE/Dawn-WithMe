import styled from 'styled-components';

const IBubbleWrapper = styled.div`
  justify-content: flex-end;
  padding-left: 47px;

  & div {
    flex-direction: row-reverse;
    margin-bottom: 10px;
  }
`;

export default IBubbleWrapper;
